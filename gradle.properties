## Project-level Gradle properties
# Allow Gradle to auto-download Java toolchain installations when requested by the build
org.gradle.java.installations.auto-download=true

# Reasonable default for Gradle JVM memory
org.gradle.jvmargs=-Xmx1g
